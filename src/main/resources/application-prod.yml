spring:
  datasource:
    url: jdbc:mysql://mysql:3306/appdb
    username: app
    password: apppass
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false